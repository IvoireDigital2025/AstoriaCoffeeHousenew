{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build && npm run db:push && cp -r attached_assets dist/public/ || echo 'No attached_assets to copy'"
  },
  "deploy": {
    "startCommand": "node start.js",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE"
  }
}